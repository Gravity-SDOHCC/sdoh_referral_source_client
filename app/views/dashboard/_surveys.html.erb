<div class="tab-pane fade <%= 'show active' if active %>" id="<%= id %>" role="tabpanel" aria-labelledby="<%= id %>-tab">
  This is the Surveys/Questionnaires tab where all questionnaires will be listed: name , fhir_resource(json in popover)
</div>
